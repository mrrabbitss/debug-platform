line-length = 110

[lint]
# Keep the repository-wide baseline explicit so Ruff upgrades do not silently
# enable unrelated rule families in the backend, tests, or maintenance scripts.
select = ["E4", "E7", "E9", "F"]

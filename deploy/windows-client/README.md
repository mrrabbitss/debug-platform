# GWAP 分机客户端

完整解压后，按包内《分机使用指南.md》操作。

- `Install.bat`：安装桌面快捷方式。
- `Start.bat`：连接服务器并打开 CodeAgent。
- `Trust Server Certificate.bat`：首次导入管理员提供的服务器证书。

需要已有可用的 CodeAgent。本包不安装后端、数据库、Python 或模型。
服务器地址、证书和个人访问令牌由管理员提供。

# 专家角色与知识协作迭代实施记录

状态：源码与本机集成完成，2026-09-09。用户确认的方案已经实施；本轮没有重新打包或发布安装器。

## 已确认范围

- 增加 EXPERT；用户管理、全局 Embedding/Reranker/GGUF 配置仍仅 ADMIN。
- 普通用户可使用 AI 案例提炼、管理自己的普通知识草稿，对共享知识和 Skill 提交变更建议。
- ADMIN/EXPERT 完整管理知识、Skill、类别和审批；审核者可多轮 AI 修正后直接批准，保存原稿、差异及审核记录。
- 私有 Chat API 仅所有者可见和使用；ADMIN/EXPERT 新建默认共享，也可私有；个人选择优先于共享默认，贯通各 AI 功能。
- 外部模型 HTTP(S) Base URL 不再受端点白名单或私网开关限制。凭据继续加密并隔离。
- 普通用户三栏，ADMIN/EXPERT 新增独立“知识库管理”，容纳 Skill/类别、AI 整理、审批与高级知识维护。
- 已审批的精确版本持久化；发布中断自动恢复；旧有效索引直到新版本构建成功才切换。
- 关闭空库自动补种旧知识。指定实例清空当前知识与 Skill 后导入用户提供的六文件组网包；保留案例、报告及必要历史引用，先备份。
- 无专属 Skill 时使用通用 Skill；两者皆无时允许仅凭日志证据诊断，明确标记缺少 Skill。

原始业务 Skill 保持在用户提供的工作区外 ZIP；不得将公司内容写入公开源码。
界面方案已确认，并已完成生产前端、实际 API 页面与定向后端检查；真实公司模型的语义质量仍需现场验收。

## 基线与工作区

- 基线：release 分支 6523cca。
- 开发分支：codex/expert-knowledge-iteration。
- 既有未跟踪 AI_PRACTICE_COMMUNITY_POST.md 与本任务无关，保留。
- 沿用项目已配置的 85% 自动上下文压缩阈值。
- 不重跑历史通过的回归、Full 或手动 CI；只运行必要的新增/改变行为检查。

## 分工与接续

| 负责方 | 范围 | 状态 |
| --- | --- | --- |
| 主线 | 中央 RBAC、助手发布恢复、Skill 必读与类别、重置导入、集成与总账 | 完成 |
| 模型代理 Singer | 模型所有权、共享选择、端点策略、0023 迁移 | 48项新检查通过；契约同步完成 |
| 知识代理 Ohm | 普通知识/Skill 边界、贡献审批、AI 提炼隔离、0024 迁移 | 62项新检查及3项受影响复核通过 |
| 前端代理 Jason | 角色导航、模型设置、知识管理及审批页面 | 24项新检查通过；主线补2项及构建通过 |
| 数据与恢复代理 Socrates | 外置备份、六文件重置与中断恢复 | 52项新场景、16项改变的助手恢复断言通过 |

代理仅编辑各自约定文件；共享 API 合同通过主线同步。没有推送、发布新版本或触发 CI。

## 已完成检查点

- [x] 中央权限与模型/知识接口集成
- [x] 发布恢复、审批幂等与并发保护（主线新增12项及改变的旧恢复断言通过）
- [x] 普通知识与必读 Skill 分离（6项类别/完整模型阅读新检查通过）
- [x] 三角色页面、真实 API 门禁及本机页面读取
- [x] 指定数据库盘点、备份、清理及六文件导入
- [x] 新增定向验证、仓库护栏与文档同步（最新结果见 VALIDATION.md）

## 2026-09-09 实际数据恢复点

读取实际 Settings 后确认当前项目使用 `backend/data/gw_ap_debug.db`，auth_mode=local；
本机默认已安装服务器目录不存在，没有将历史测试实例视作目标。
先创建 Git 外 SQLite 一致备份，再以副本验证 0016→0024 迁移，最后迁移真实目标。
备份目录为 `D:/GRXM/backups/expert-iteration-20260909`，其中 `before-migration-manifest.json`
记录迁移前备份与案例/分析/报告/附件旧列的计数和 SHA-256；没有在本文件保存业务正文或端点。

已按此前用户批准执行一次 `expert-iteration-20260909-hilink`：六文件归类、人工确认、
保留原稿及根文件平台适配差异、持久任务、完整向量/图谱构建、最终原子切换。
任务 `JOB-b00cd770d9464b5c` 为 COMPLETED；新图谱 `KGEN-d02d6449c9b0491d`。
活动库由旧4文档替换为6份组网 Skill；旧正文/版本/分块作为历史保留，不再用于新诊断。
14案例、16分析、28附件及0独立报告记录的旧列哈希全部不变。
全文方法加载获得6文件、0缺失依赖，默认模板指向导入的报告规范。
本次实际索引使用现有本地 hashing 配置，没有调用 Chat 模型或发送公司 Skill 正文到模型端点。

导入后使用新进程启动完整 FastAPI lifespan，bootstrap 和 knowledge HTTP 读取通过：
仍为6份已发布 Skill；network=DEDICATED，connection/general=EVIDENCE_ONLY，unknown=CROSS_CATEGORY。
没有自动补回旧知识。SQLite quick_check=ok，外键检查无错误。

## 主线新增验证记录

- `test_expert_publication_recovery.py`：12项新检查通过，模型固定接入后重新验证仍通过。
- `test_skill_scope_iteration.py`：6项新检查通过；首次内置类别改名版本号失败，修复为持久化基础版本后递增，定点重检通过。
- `test_assistant_model_binding_iteration.py`：5项新检查通过；停用发起者发生在领取阅读前时收尾状态曾停留READING，已修正并定点验证为FAILED且零模型请求；整理普通 Wiki 不再被隐式转换成必读 Skill。
- 既有助手全文文件夹检查因模型签名/快照改变定点验证1项通过；VIEWER个人模型选择定点1项通过。
- 这些检查使用合成资料和假 Chat，不证明真实公司模型的语义诊断准确率。
- 重置模块52个不同新增场景由独立代理增量验证，包含真实进程异常退出后使用同审批恢复；详见重置交接。

## 最后集成与使用形态

- 专家晋升保留原识别码及一次性网页登录票据；服务端1项晋升检查和真实 PowerShell 分机脚本9条断言通过。
  新分机源码接受 ENGINEER/EXPERT 并复用已保存令牌，拒绝越权角色和错配身份，未改写用户实际凭据。
- `test_expert_workbench_integration.py` 在完整 REST 路由和中央 RBAC 上验证专家新类别立即进入普通用户案例选择，
  改名/停用保留旧案例类别，用户管理保持管理员专属。
- Markdown 导入、审核 AI、整理助手和各诊断入口使用个人优先的固定 Chat 快照；排队后模型变更或停用终止旧请求。
- `workflow_recovery.py` 已统一接入普通贡献、旧发布、整理助手和重置的恢复 hook。
  新增 dispatcher 检查覆盖取消租约到期后同事务释放自己占用的构建代，保留旧索引和审批。
- 案例/报告提交与知识审核贡献同事务保存；中途异常全部回滚。HTTP新增2项通过，前端新增1项及受影响兼容路径1项通过。
  首次检查仅幂等桥接返回码断言误写为200，按实际201契约修正后定点通过；没有遗留失败。
- 最新 `npm run build` 包含 vue-tsc，通过；26个不同新浏览器场景均有通过证据，包括专家访问原有候选经验审核页。
  浏览器模拟检查不连接业务数据库。另以实际本机数据库启动完整后端与生产前端，查看知识管理六文件、审批中心、
  AI整理助手及统一模型设置，页面无 JavaScript 错误，无 Chat 请求；查看后停止临时核验服务。

管理角色从左侧“知识库管理”直接进入 Skill/类别、AI整理助手、审批中心或知识维护；
普通用户从“知识库”进入 Wiki、案例与报告、只读 Skill、我的提交，并可使用 AI 案例提炼。
系统设置的“我的统一模型选择”控制此人之后的诊断、问答和各 AI 流程；运行中的任务维持原快照。

本轮本机实际导入只影响当前项目数据库，不表示另一台电脑上的0.4.0服务器已升级。
Git 外备份和导入清单见上文；业务 Skill、数据库及模型凭据没有进入源码变更。
未运行历史全量回归、Full、手动CI、新安装包构建或真实公司模型推理；没有宣称新版合并/发行门禁已满足。
最终仓库护栏24/24通过，196 Python与36 Vue文件架构边界通过；78个改变/新增Python文件中，76个清洁，
两个旧助手测试文件28条Ruff诊断与HEAD基线一致。已修正此次发现的1处未使用导入；差异和敏感数据检查通过。

# 三栏工作台改版与接续记录（2026-09-08）

## 已确认范围

用户已确认完成全部需求，允许前端、知识助手与报告子代理并行。
主导航仅故障定位、知识库、系统设置；普通用户只读已发布知识，可提交案例和报告，由管理员确认共享。
管理员维护分类、知识与模板；对具体知识变更清单的一次确认即发布审批，索引成功后全员生效。
新案例与知识助手模型出站默认开启，历史关闭记录不改；普通用户可选管理员预设模型。
类别为组网、连接、未知，另有通用知识。用途含日志分析、综合诊断、故障树、报告格式、先验知识。
未知跨类检索并建议类别；跨类补充说明原因；运行中固定知识和模板版本。
完整读取Skill目录及依赖，先给归位/新增/合并/替换/关联/跳过清单和差异，再对话纠偏确认。
报告保留组网总览、异常设备时间轨迹、逐AP三层分析与因果链、结论与后续四章。
页面移除事件与时间线入口，报告保留时间轨迹，保留交互问答。

## 起点与恢复

- 源码HEAD：`46c3b1f47529ab17095d5e4e01ef8bb6ea59b671`，分支 `release`。
- 571文件源码恢复快照：`artifacts/baselines/20260908T103534Z`（含未跟踪源码，不含业务数据和凭据）。
- 原始任务末次代码写入2026-09-08 17:02，后续远程上下文压缩连续失败。已有代码未经本次改版验证。
- 无关文稿 `AI_PRACTICE_COMMUNITY_POST.md` 保留，不纳入交付。
- 项目Codex配置将自动压缩阈值设为219,640 tokens（实测258,400窗口的85%）。当前运行轮次是否热加载由客户端决定；不宣称可以手动强制压缩或保证网络成功。

## 执行分工及当前状态

| 项目 | 负责人 | 状态 |
| --- | --- | --- |
| 分类/权限/知识与模型快照、Skill依赖与各诊断入口一致性 | 主代理 | 完成，新增检查及包内联调通过 |
| 三栏前端、案例简化、角色区别与知识助手界面 | 前端子代理 | 完成，构建及9项模拟浏览器检查通过 |
| 知识助手完整阅读/对话/计划/原子发布/恢复 | 助手子代理 | 完成，56项独立新增检查通过 |
| 四章模板及HTML/Word/PDF一致性 | 报告子代理 | 完成，41项检查通过，PDF视觉核验通过 |
| 新增定向检查、仓库护栏、文档、离线安装包与指南 | 主代理汇总 | 完成，EXE/分机ZIP/指南已交付，检查范围见VALIDATION |

## 验证约定

不重复已通过的历史回归，不自动跑Full，不手动触发CI。
仅检查本次新增或改变行为；历史验证只证明原范围，不能作为本次改版通过证据。
使用合成材料和隔离数据库，不外发公司数据，不读取或回显模型密钥。
新安装包必须做完整性与来源核对；公司实机、代码签名、CI门禁分别陈述范围。

## 下次接续入口

先读本记录、`git status --short --branch`、各子代理最终结果，再继续未完成项。
不要重置工作区，不重复已记录通过的检查，不将半成品标记可发布。

## 主线检查点

- 已实现内容寻址的知识/分块快照与运行配置快照，避免配置脱敏器截断正文或50项列表后损坏历史上下文。
- 网页综合诊断、日志规划、问答、修订、Host CLI开始/恢复/最终报告接入固定配置。
- 总领Skill读取自动包含已确认依赖，支持循环去重、缺失引用提示、跨类原因。
- 已确认案例/报告作为历史知识加入检索，待管理员确认的材料不会被新诊断使用。
- 新增 `test_workbench_snapshot.py` 六项检查通过（20.05秒）；证据 `artifacts/validation/workbench-20260908/snapshot.xml`。
- 主程序OpenAPI可生成，含130个路径及工作台入口。未启动业务数据库。
- 其后增加同代图谱兼容检查（保持当前图谱可用，发布切代后使用固定正文检索）及管理员默认模板选择，仍需定向检查。

### 后续进展

- 同代图谱候选版本/跨类说明、默认模板新旧运行隔离、0021升级保留历史关闭授权三项检查通过；个人预设模型不影响全局配置检查通过。
- 新增快照检查累计10项通过（分组执行，原失败的模型测试fixture字段已纠正）。架构检查通过；未运行历史完整回归。
- 四个并行子代理：前端 `01a08097-d5e7-7771-82cb-62bd0639883e`，助手 `01a08097-d6ff-7b40-8578-c0a020021177`，报告 `01a08097-d829-73a1-8057-01e28d2f72d2`，权限 `01a08098-8177-7300-bef1-845775f3acb4`。
- 前端和权限代理出现一次网络断流，已向原代理发送接续请求；保留已写入代码。
- 服务器/分机指南已更新为0.4.0流程；安装器尚未构建，不将指南中的目标产物视作已完成。

### 2026-09-09 恢复执行

- 用户再次明确“继续完成”，沿用全部已确认范围和并行授权。旧子代理已失效，已建立替代代理，继续使用同一工作区。
- 前端 `01a081ec-664b-7360-9529-cb48c028ebdc`，助手 `01a081ec-672a-76b1-9e9b-811a611486cb`，报告 `01a081ec-6824-70f2-b256-27c0844a0a2f`，权限 `01a081ec-6902-7750-9d88-02a5b6ef9115`。
- 主线已接入助手任务恢复钩子、统一内置模板版本2、四个报告生成/修订入口的固定模板与案例证据检查。
- 修正健康检查路径误匹配导致的鉴权绕过；旧知识路由与审批入口明确仅管理员。
- 案例库提交流程移入领域服务：验证案例存在、负责人身份、完成的关联诊断，管理员版本锁确认；共享检索排除未发布与受限资料。
- 新增 `test_workbench_flows.py` 六项检查通过（19.43秒）；覆盖工作台API、提交确认冲突、来源校验、运行/修订快照及检索隔离。证据 `artifacts/validation/workbench-20260909/flows.xml`。
- 仍待并行模块完成、主线最终联调及新安装包构建。当前不作为已完成交付。

### 收尾检查点

- 前端及权限已完成，见各交接记录：生产构建与9项模拟浏览器检查、25项独立权限检查通过。
- 报告41项新增独立检查通过，四页合成PDF已视觉核验；Word结构检查通过，缺少LibreOffice，未完成实际分页视觉核验。
- 主线新增流程累计11项通过：原6项，删除预设模型拒绝1项、实际助手依赖清单与自定义类别2项、图谱路径契约1项、案例库固定报告1项。失败的报告夹具已在最终代码下单项复核通过（6.25秒，`report-library-final.xml`）。
- 任务执行器修复事务成功后误报租约丢失；新增1项检查通过（6.11秒，`transactional-job.xml`），保留运行中的租约与取消校验。
- 架构检查当前通过（182个Python、28个Vue文件，最大复杂度50）；最终知识助手源码完成后汇总仓库护栏。
- 分机ZIP已构建：`artifacts/lan/workbench-0.4.0/client/GWAP-Client-b1d91dda4f932e2f.zip`，24文件、44858字节。服务器离线EXE尚未构建。
- 助手代码已冻结，56项独立新增检查通过。仓库护栏与最终架构检查通过，证据为 `repo-harness.json`、`architecture.json`。
- 任务主线再增加3项接管隔离和1项配置损坏终止检查，5项新增任务检查累计通过，避免旧任务异常覆盖接管者、避免请求一直显示运行中。
- Core已构建完成：`artifacts/portable/workbench-0.4.0/debug-platform-windows-x64`。GGUF整合正在进行；不复跑历史模型smoke，组件仍做完整哈希校验。

### 最终交付状态（2026-09-09）

本次已确认的本地开发及安装包交付全部完成。前述“实施中/待构建”为过程记录，以本节为准。
四个替代子代理均已完成并关闭。构建完成时源改动保留在工作区，未提交、推送或触发CI；无关文稿保留。

- 交付目录：`artifacts/lan/workbench-0.4.0/delivery`。服务器EXE、分机ZIP、两份指南、SHA256与交付说明见[0.4.0交付记录](../deploy/releases/20260909/0.4.0/README.md)。
- 服务器完整载荷10,181文件校验通过；257个后端/前端文件与当前源码一致；分机24文件清单与ZIP/指南/Skill一致。
- 全新隔离数据库使用包内Python启动、迁移到0022、访问三栏页面、创建/更新案例、模拟诊断、HTML/DOCX/PDF导出及下载哈希检查全部通过。
- 关联报告由服务端生成并固定，提交后管理员确认成功；明确关闭授权的助手保持暂停，不能无授权重试；重启后案例设置、共享确认和暂停会话均保留。
- 证据：`artifacts/validation/workbench-20260909/package-runtime/result.json`；未调用外部模型，未启动或修改现有业务服务器数据。
- 新增检查及其限制已汇总到 `VALIDATION.md`。不将本地合成检查代称真实公司模型、公司网络/实机、Word实际分页、代码签名或CI合并验收。

### GitHub分发（2026-09-09）

用户明确要求把0.4服务器、配套分机包和使用说明推到GitHub Release。
分发目标为 `v0.4.0`，对应本轮源码提交；附件沿用原EXE和分机ZIP，只统一外部文件名并补充独立升级说明。
复核原交付SHA-256及257项源码对应关系，不重复既有回归或手动CI。
0.3.3无需卸载；原Windows账号下停止、备份后覆盖安装，保留原数据目录和配置，启动迁移后重建向量索引。
文件与验证边界见[0.4.0交付记录](../deploy/releases/20260909/0.4.0/README.md)。
